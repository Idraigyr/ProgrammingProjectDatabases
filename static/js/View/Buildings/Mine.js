import {Building} from "../BuildingView.js";
import * as THREE from "three";

/**
 * Mine building view
 */
export class Mine extends Building{
    constructor(params) {
        super(params);
    }
}